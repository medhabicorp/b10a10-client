import React from "react";
import PropTypes from "prop-types";

const Pricing = (props) => {
  return (
    <div>
      <h1>Pricing</h1>
    </div>
  );
};

Pricing.propTypes = {};

export default Pricing;
