import React from "react";
import PropTypes from "prop-types";
import Banner from "../Banner/Banner";

const Home = (props) => {
  return (
    <div>
      <div>
        <Banner />
      </div>
    </div>
  );
};

Home.propTypes = {};

export default Home;
